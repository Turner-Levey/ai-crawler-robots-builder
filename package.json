{
  "name": "ai-crawler-robots-builder",
  "version": "1.0.0",
  "private": true,
  "description": "No-tracking browser utility for AI crawler robots.txt rules.",
  "license": "MIT",
  "scripts": {
    "check": "node --check script.js && node --check submit-indexnow.mjs"
  }
}
